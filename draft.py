# МОДУЛЬ 1. Фундамент та автоматизація рутини
#Теорія: Базовий синтаксис, типи даних (рядки, числа, списки, словники), цикли (for, while), умови (if/else), функції.
#Практика: Написання скрипта для автоматичного сортування файлів за розширеннями у директорії або програма для читання та фільтрації даних у Excel-документах.

#Наприклад, так ми створюємо 
# А щоб програма показала нам, що лежить у цій коробці, ми використовуємо вбудовану команду print():
# Давайте почнемо писати наш калькулятор. Уявімо, що ваш дохід становить 1000 (умовних одиниць), а ставка податку — 5 (відсотків).

income = 1000
tax_percent = 0.05
print(income, tax_percent)

#Як би ви написали рядок коду, щоб створити нову змінну net_profit (чистий прибуток) і розрахувати її значення за допомогою ваших математичних операцій та вже існуючих змінних income і tax_percent?


def calculate_net_profit(income, tax_percent):
    net_profit = income - (income * tax_percent)
    corporate_net_profit = net_profit * 0.8
    return corporate_net_profit

income = 1000
tax_percent = 0.05
net_profit = calculate_net_profit(income, tax_percent)
print(net_profit)

#Зараз наша програма завжди рахує прибуток лише для числа 1000. Щоб зробити її по-справжньому корисною, вона має запитувати суму доходу безпосередньо у користувача.
#Для цього в Python є команда input(). Наприклад, щоб запитати ім'я, ми пишемо:

name = input("Введіть ваше ім'я: ")

#Але тут є один важливий нюанс: усе, що користувач вводить через input(), Python автоматично сприймає як звичайний текст (навіть якщо це цифри). Ми не можемо віднімати або множити текст.
#Щоб перетворити текст на число, ми використовуємо спеціальні команди:
#int() — для цілих чисел (наприклад, 1000).
#float() — для чисел з крапкою (наприклад, 1000.50).

#Це можна зробити одразу в одному рядку, обгорнувши input у функцію перетворення:
#age = int(input("Введіть ваш вік: "))
#Спираючись на цей приклад, як би ви оновили перший рядок вашого коду (income = 1000), щоб програма просила користувача ввести суму доходу з клавіатури і відразу перетворювала її на число з крапкою (float)?

income = float(input("Введіть ваш дохід: "))

#Напишіть, будь ласка, як тепер виглядає весь фінальний код вашої програми від початку до кінця?

income = float(input("Введіть ваш дохід: "))
tax_percent = 0.05
net_profit = calculate_net_profit(income, tax_percent)
print(net_profit)

#Форматовані рядки зручно вставляти значення змінних прямо всередину тексту. Для цього перед початком тексту (перед лапками) ставиться літера f, а сама змінна береться у фігурні дужки {}.
#Ось приклад:print(f"Ви вказали дохід у розмірі {income}")

print(f"Ваш чистий прибуток становить: {net_profit}")

#if умова:
    # код, який виконається, якщо умова правдива
#else:
    # код, який виконається у всіх інших випадках

if income < 10000:
    tax_percent = 0
else:
    tax_percent = 0.05

income = float(input("Введіть ваш дохід: "))
if income < 10000:
    tax_percent = 0
else:
    tax_percent = 0.05
net_profit = income - (income * tax_percent)
print(f"Ваш чистий прибуток становить: {net_profit}")

#Робота з наборами даних.
#У реальному житті ми часто маємо справу не з одним числом, а з цілим списком чисел. Щоб зберігати багато значень одночасно в одній змінній, у Python використовують списки (lists). Вони створюються за допомогою квадратних дужок [], а елементи всередині розділяються комами.
order_prices = [1200, 8500, 400]

#Цикл for дозволяє нам виконувати певний код для кожного елемента в наборі даних (наприклад, у списку). Наприклад, якщо у нас є список цін на товари, ми можемо по черзі обробити кожну ціну за допомогою циклу for.
#for price in order_prices: 
    # тут пишемо код, який виконається для кожної суми
    # змінна price по черзі буде дорівнювати 1200, потім 8500, потім 400

for price in order_prices:
    if price < 10000:
        tax_percent = 0
    else:
        tax_percent = 0.05
    net_profit = price - (price * tax_percent)
    print(f"Ваш чистий прибуток становить: {net_profit}")

#Python має вбудовані команди (їх називають методами), які вміють змінювати текст
#strip() - видаляє пробіли з початку та кінця рядка
#lower() - перетворює всі літери в рядку на малі
#upper() - перетворює всі літери в рядку на великі

item = " КУРТКА " .lower().strip()

cart = [" КУРТКА ", "  шапка", "Джинси "]
for item in cart:
    print(item.strip().lower())

#МОДУЛЬ 2. Збір інформації з інтернету (Web Scraping)
#Теорія: Робота з HTTP-запитами, бібліотеки requests та BeautifulSoup, розуміння структури веб-сторінок.
#Реальна практика: Створення парсера. Завдання: автоматично зібрати ціни на товари з сайту-конкурента та зберегти їх у зручному форматі (CSV або JSON).
try:
    import requests
    from bs4 import BeautifulSoup
except ImportError:
    requests = None
    BeautifulSoup = None
    print("Модуль requests або bs4 не встановлено. Використовується urllib для веб-запитів.")

if requests:
    response = requests.get("https://example.com")
    print(f"{response.status_code}")
else:
    import urllib.request
    with urllib.request.urlopen("https://example.com") as response:
        print(response.status)

#Тепер, коли ми успішно "зайшли" на сторінку, нам потрібно отримати її вміст (увесь той HTML-код, з якого складається сайт). Уся ця текстова інформація зберігається у властивості text нашої відповіді.
#Як би ви написали рядок коду, щоб створити нову змінну html_content і зберегти в неї текст нашої сторінки (використовуючи response.text)?

html_content = response.text

#А щоб "зварити наш суп" (тобто створити об'єкт, який буде шукати для нас дані), ми передаємо йому наш HTML-текст і кажемо, що це саме HTML-сторінка за допомогою команди "html.parser". Шаблон виглядає так:
#soup = BeautifulSoup(текст_сторінки, "html.parser")
soup = BeautifulSoup(html_content, "html.parser")

#Щоб знайти цей шматочок тексту за допомогою BeautifulSoup, ми використовуємо метод find() 🔍. Йому потрібно передати назву тегу та назву класу.
#Шаблон виглядає так:soup.find("назва_тегу", class_="назва_класу")

price_element = soup.find("span", class_="product-price")
print(f"Ціна товару: {price_element.text}")

#Але клієнту (або нашій базі даних) потрібна лише сама ціна, тобто текст "1500 грн", без цих HTML-тегів. Щоб витягти тільки чистий текст із знайденого елемента, ми просто звертаємося до його властивості text.
#Наприклад: price_element.text

#Давайте зберемо наші знання до купи. У нас вже є код, який:

#- "Стукає" на сайт і завантажує сторінку (requests.get).
#- Отримує HTML-текст сторінки (response.text).
#- Перетворює його на зручну структуру (BeautifulSoup).
#- Знаходить потрібний елемент із ціною (soup.find()).

try:
    import requests # type: ignore[reportMissingModuleSource]
    from bs4 import BeautifulSoup # type: ignore[reportMissingModuleSource]
except ImportError:
    requests = None
    BeautifulSoup = None
    print("Модуль requests або bs4 не встановлено. Використовується urllib для веб-запитів.")

if requests:
    response = requests.get("https://example.com")
    print(response.status_code)
else:
    import urllib.request
    with urllib.request.urlopen("https://example.com") as response:
        print(response.status)
html_content = response.text
soup = BeautifulSoup(html_content, "html.parser")
price_element = soup.find("span", class_="product-price")
print(f"Ціна товару: {price_element.text}")

#Вітаю, ви щойно написали свій перший веб-скрейпер! Збір даних з сайтів — це дуже затребувана навичка на ринку.

#Модуль 3: Месенджери та комерційні боти
#Теорія: Основи об'єктно-орієнтованого програмування (ООП), робота зі сторонніми API, асинхронне програмування (asyncio), фреймворк aiogram.
#Практика: Розробка Telegram-бота для малого бізнесу (наприклад, служби доставки), який приймає замовлення від клієнтів, розраховує вартість кошика та надсилає деталі менеджеру.

#Уявімо нову задачу: служба доставки хоче Telegram-бота, який прийматиме замовлення від клієнтів. Ми будемо писати його за допомогою сучасної та потужної бібліотеки aiogram.
#Але перш ніж писати логіку, програмі потрібно якось зв'язатися з серверами Telegram і довести, що вона має право керувати саме вашим ботом. Для цього Telegram видає розробникам унікальний цифровий підпис (пароль), який називається API Token (він виглядає приблизно так: "123456789:ABCdefGhIJKlmNoPQRstUVwxyZ").
#Спираючись на наші знання з першого модуля, як би ви написали рядок коду, щоб створити змінну з назвою BOT_TOKEN і зберегти в неї цей текстовий пароль-токен?

BOT_TOKEN = "123456789:ABCdefGhIJKlmNoPQRstUVwxyZ"

#Щоб підключитися до Telegram, бібліотека aiogram використовує два головні інструменти:
#Bot — це сам "кур'єр", який має доступ до Telegram завдяки нашому токену і вміє відправляти повідомлення.
#Dispatcher (диспетчер) — це "менеджер", який сортує всі вхідні повідомлення від користувачів і вирішує, що з ними робити.
#Ось як виглядає їх базове налаштування:
#Python:
import aiogram
from aiogram import Bot, Dispatcher

bot = Bot(token=BOT_TOKEN)
dp = Dispatcher()




#Модуль 4: Зберігання та управління даними
#Теорія: Реляційні бази даних (SQL), робота з SQLite та PostgreSQL, використання ORM (наприклад, SQLAlchemy) для взаємодії з базою через код.
#Реальна практика: Інтеграція бази даних у нашого Telegram-бота, щоб зберігати історію замовлень, контактні дані клієнтів та генерувати автоматичні звіти про продажі за місяць.


